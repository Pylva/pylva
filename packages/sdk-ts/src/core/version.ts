/** Must match packages/sdk-ts/package.json; package artifact smoke enforces it. */
export const SDK_VERSION = '1.2.0' as const;

/** Canonical workspace plan/access-state response contract consumed by this SDK. */
export const API_CONTRACT_VERSION = '2' as const;
